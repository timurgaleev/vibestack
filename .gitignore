SYNC_GUIDE.md
bin/.vibe-render-skill.bak.*
.env
node_modules/
dist/
# The diagram-render bundle is a committed build artifact (resolved at runtime).
!lib/diagram-render/dist/
!lib/diagram-render/dist/**
.vibestack/
.claude/skills/
.claude/scheduled_tasks.lock
.claude/*.lock
.claude/settings.local.json
.agents/
.factory/
.cursor/
.opencode/
.context/
*.log
*.bun-build
.env
.env.local
.env.*
!.env.example

# Fail-on-match renderer stubs from test-install-integration.sh. The suite
# sweeps its own, but a `git add -A` racing a run would otherwise commit one.
bin/.vibe-render-skill.stub.*

# py_compile output from local syntax checks
__pycache__/
